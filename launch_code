#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [ -n "${DISPLAY:-}" ] || [ -n "${WAYLAND_DISPLAY:-}" ]; then
  exec python3 "$SCRIPT_DIR/launcher_ui.py" "$@"
fi

exec "$SCRIPT_DIR/start_controller.sh" "$@"
